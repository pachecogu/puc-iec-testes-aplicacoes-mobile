# Copie para .env e ajuste.
PORT=4000
# Segredo de assinatura do JWT. Gere um valor aleatório forte em produção.
JWT_SECRET=dev-secret-change-me
# (opcional) token TMDB v4 para sincronizar catálogo real. Sem ele, usa só o seed local.
TMDB_TOKEN=
