{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Demo de mutation testing da Aula 3. Escopo só na lógica pura (store + utils) — rápido e sem RNTL. Rode DEPOIS de resolver os testes (precisa de testes passando pra ter mutation score).",
  "testRunner": "jest",
  "coverageAnalysis": "perTest",
  "mutate": [
    "src/store/**/*.ts",
    "src/utils/**/*.ts"
  ],
  "jest": {
    "projectType": "custom",
    "configFile": "jest.config.mutation.js"
  },
  "reporters": ["html", "clear-text", "progress"],
  "concurrency": 2
}
