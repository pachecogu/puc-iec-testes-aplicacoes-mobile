# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env
.env*.local

# test artifacts
coverage/
results.json
reports/
.stryker-tmp/

# typescript
*.tsbuildinfo

# generated native folder (iOS não usado no lab — Maestro/build android/ é o que roda em aula)
/ios

# android/ é COMMITADO (lab de security requer rebuild local — ver lab-perf-security.md).
# Só build cache/local config ficam fora:
android/app/build/
android/.gradle/
android/app/.cxx/
android/local.properties
